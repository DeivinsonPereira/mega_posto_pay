import 'package:flutter/material.dart';

import '../utils/static/custom_colors.dart';

abstract class CustomDeleteButtom {
  static Icon iconData =
      const Icon (Icons.delete_sharp, color: CustomColors.containerQuantity);
}
